{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run database_test",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Release/database_test.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "Build database_test",  // This ensures it builds before running
            "console": "integratedTerminal"
        }
    ]
}
